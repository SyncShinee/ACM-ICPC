#include <cstdio>
#include <algorithm>
#include <cstring>
#include <vector>
#define lson l, mid, rt << 1
#define rson mid + 1, r, rt << 1 | 1
using namespace std;
struct node {
    
}
int main() {
    int n;
    for (int i = 1; i < n; ++ i) {
        scanf("%d%d%d", &x, &y, &z);
        G[x].push_back(node(x, y));
    }
    return 0;
}
